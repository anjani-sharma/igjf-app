{
  "name": "igjf-app",
  "version": "1.0.0",
  "description": "IGJF Application with backend and mobile app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install",
    "postinstall": "npm run install-backend"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}